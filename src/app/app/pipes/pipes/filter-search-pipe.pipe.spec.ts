import { FilterSearchPipePipe } from './filter-search-pipe.pipe';

describe('FilterSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
